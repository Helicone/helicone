import type { FileWithImports } from '@mintlify/common';
import type { OpenApiFile } from '@mintlify/models';
import type { PageType } from '../../types/PageType.js';
export declare class UpdateOrchestrator {
    readonly contentDirectoryPath: string;
    readonly staticFilenames: string[];
    readonly openApiFiles: OpenApiFile[];
    readonly contentFilenames: string[];
    readonly snippets: string[];
    readonly snippetV2Filenames: string[];
    pagesAcc: Record<string, PageType>;
    filesWithImports: FileWithImports[];
    private constructor();
    static update(contentDirectoryPath: string, staticFilenames: string[], openApiFiles: OpenApiFile[], contentFilenames: string[], snippets: string[], snippetsV2Filenames: string[]): Promise<import("@mintlify/models").ConfigType & {
        repo?: {
            github?: {
                owner: string;
                repo: string;
                deployBranch: string;
                contentDirectory: string;
                isPrivate?: boolean | undefined;
            } | undefined;
        } | undefined;
    }>;
    private update;
    private readFiles;
    private writeMdxFilesWithNoImports;
    private updateOpenApi;
}
