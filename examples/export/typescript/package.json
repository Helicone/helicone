{
  "name": "helicone-data-export",
  "version": "1.0.0",
  "description": "A command-line tool to export request/response data from Helicone's API",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "helicone",
    "export",
    "data",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
