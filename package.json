{
  "name": "helicone-monorepo",
  "private": true,
  "workspaces": [
    "bifrost",
    "web",
    "packages/*",
    "valhalla/jawn"
  ],
  "scripts": {
    "build:web": "yarn workspace helicone build",
    "build:cost": "yarn workspace @helicone-package/cost build",
    "build:llm-mapper": "yarn workspace @helicone-package/llm-mapper build",
    "build:types": "yarn workspace @helicone-package/types build",
    "build:packages": "yarn build:types && yarn build:cost && yarn build:llm-mapper"
  },
  "engines": {
    "node": "=20"
  }
}
