/**
 * Mistralai endpoint configurations
 */

import type { ModelEndpoint } from "../../types";

export const mistralaiEndpoints: Record<string, ModelEndpoint[]> = {};

export default mistralaiEndpoints;
